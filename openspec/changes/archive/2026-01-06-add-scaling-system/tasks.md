## 1. Library Structure

- [x] 1.1 Create `src/` directory for library source code
- [x] 1.2 Update `package.json` with library entry point and TypeScript config

## 2. Core Scaling Function

- [x] 2.1 Implement `s()` function in `src/scaling.ts`
- [x] 2.2 Implement `getScaleFactor()` utility for debugging/testing
- [x] 2.3 Add TypeScript types for scaling functions

## 3. Design Tokens

- [x] 3.1 Implement `space` tokens (xs, sm, md, lg, xl, xxl) in `src/tokens.ts`
- [x] 3.2 Implement `font` tokens (caption, body, subtitle, title, headline) in `src/tokens.ts`
- [x] 3.3 Implement `radius` tokens (none, sm, md, lg, full) in `src/tokens.ts`

## 4. Scaled Style Utility

- [x] 4.1 Implement `createScaledStyle()` function in `src/scaling.ts`

## 5. Library Exports

- [x] 5.1 Create `src/index.ts` with public API exports

## 6. Integration Example

- [x] 6.1 Update example app screen to demonstrate scaling usage
